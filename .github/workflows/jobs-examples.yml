name: using jobs

on: 
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "Build Success" 

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: deploy
      run: echo "Deployed" 
